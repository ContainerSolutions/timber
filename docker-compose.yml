version: '3'

services:
  backend:
    build: backend
    ports:
    - 8080
    volumes:
    - ./backend:/app
  frontend:
    build: frontend
    ports:
    - 8080
    volumes:
    - ./frontend:/app
  redis:
    image: redis:alpine
