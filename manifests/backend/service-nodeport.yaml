---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: timber-backend
  name: timber-backend
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: timber-backend
  type: NodePort
